//package funsets

//import FunSets
//import funsets.FunSets
import FunSets._

object set_test {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(107); 
	val x = 3;System.out.println("""x  : Int = """ + $show(x ));$skip(27); 
  val s1 = singletonSet(1);System.out.println("""s1  : <error> = """ + $show(s1 ))}
}
