package week6

object nqueens {
	def queens(n: Int): Set[List[Int]] = {
		def placeQyeens(k: Int): Set[List[Int]] =
			if (k == 0) Set(List())
			else
				for {
					queens <- placeQyeens(k-1)
				
				}
		placeQyeens(n)
	}


}