package week6
import vector._

object pairs {
	val n = 7                                 //> n  : Int = 7
	
		//(1 until n) flatMap (i => (1 until i) map (j => (i,j))) filter (pair =>isPrime(pair._1, pair._2)
		
	                                          //> res0: Int = 10
}